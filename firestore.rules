rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 文字雲集合：任何人可讀寫（課堂 demo 用）
    match /wordcloud_words/{document} {
      allow read, write: if true;
    }

    // 其他集合預設禁止
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
